# Minimal server settings (examples)
HOST=0.0.0.0
PORT=3001
NODE_ENV=development

# MongoDB connection string (required for employee persistence)
MONGODB_URI=mongodb://localhost:27017/hello_world_api
