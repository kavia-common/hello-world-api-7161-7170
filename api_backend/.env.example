# Server
PORT=3001
HOST=0.0.0.0

# Auth (JWT)
# REQUIRED for POST /login to work
JWT_SECRET=replace-with-a-strong-secret

# Optional token expiry (default: 1h). Examples: 15m, 1h, 7d
JWT_EXPIRES_IN=1h
