# Required for authentication + MongoDB
MONGODB_URI=mongodb://localhost:27017/minimal_express_app
JWT_SECRET=change_me_to_a_long_random_secret

# Optional auth settings
JWT_EXPIRES_IN=1h

# Existing server settings (examples)
HOST=0.0.0.0
PORT=3001
NODE_ENV=development
